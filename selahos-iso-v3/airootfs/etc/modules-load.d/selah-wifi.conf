# SelahOS: intentionally empty.
# Broadcom Wi-Fi drivers are loaded by selah-wifi-driver.service,
# which picks brcmfmac or wl per chip. Force-loading a driver here
# breaks whichever Macs need the other one.
